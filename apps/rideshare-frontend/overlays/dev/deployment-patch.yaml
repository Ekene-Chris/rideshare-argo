apiVersion: apps/v1
kind: Deployment
metadata:
  name: rideshare-frontend
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: rideshare-frontend
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
