apiVersion: v1
kind: Service
metadata:
  name: rideshare-matching-service
  labels:
    app: rideshare-matching-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: rideshare-matching-service
